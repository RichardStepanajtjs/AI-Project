server {
    listen 4200;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: send unknown paths to index.html for the Angular router
    location / {
        try_files $uri $uri/ /index.html;
    }
}
